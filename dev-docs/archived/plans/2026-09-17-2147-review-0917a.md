# Checking Team レビュー: 0917a vs main

- レビュー日時: 2026-09-17 21:47 (JST)
- 対象: ブランチ `0917a`（HEAD `df69c13d`）vs `main` — 3ラウンド16件の PBI 実装（84ファイル / +3,921 -1,051行）
- 実行方式: 並列（Wave 1: 5観点 / Wave 2: 4クラスタ13観点 / Wave 3: Test Experts）
- 検証: type-check PASS / lint 0 errors / test **12,214 passed**（770 files）/ build PASS / lint:adr-links PASS / lint:layers-docs PASS

## 総合評価: 96/100 (ランク: S)

実行された19観点（Test Experts 込み）のスコア単純平均 95.5。観点別: Red Team 70（1 High + 2 Medium）/ Maintainability 85（3 Medium）/ Code Quality クラスタ 75-100（Medium 5）/ Blue・Legacy・Ops 95（Medium 1ずつ）/ その他 100。**指摘は Wave 3（Test Experts）が全て修正済みで、上記の検証は修正後の状態です。**

## 重要指摘事項（優先度順）— 全て修正済み

### [High] 🎯 PBI-15 の boolean 伝播が OfflineNetworkQueue / StepExecutor で握りつぶされる
- 指摘者: Red Team Leader（🎯 Blue Team・Ops/Performance・Code Quality・Legacy Bridge も同一問題を指摘）
- 場所: src/background/offlineNetworkQueue.ts / src/background/pipeline/stepExecutor.ts
- 影響: 永続化失敗の boolean を無視すると、 silent loss + 「成功」INFO ログの二重嘘になる（PBI 15 が意図した失敗伝播が最上位で途絶える）
- 対処（実施済み）: `OfflineNetworkQueue.enqueue` を `Promise<boolean>` 化して port の戻り値を返却（成功時のみ INFO、失敗時 ERROR）、`StepExecutor.enqueueOfflineJob` は false 受領時に ERROR ログ。`offlineQueueFacadeFailure.test.ts`（9 tests）で pin

### [Medium] 🎯 dequeue/peek が mutate=false でもジョブを返す fail-open
- 指摘者: Red Team Leader（🎯）
- 場所: src/background/offlineNetworkQueue.ts（dequeue/peek）
- 影響: ストレージ保存に失敗したジョブがメモリから取り除かれて返り、重複実行・二重送信の可能性
- 対処（実施済み）: fail-closed 化 — mutate false 時は in-memory の take を破棄して null + WARN、スナップショットは untouched のまま次サイクル再試行

### [Medium] 🎯 Markdown SSOT が export 経路の弱いタグ sanitization を固定化
- 指摘者: Red Team Leader（🎯）
- 場所: src/utils/markdownFormatter.ts（sanitizeTagPrefix）
- 影響: タグ内の改行がリンク構文をまたいで注入されうる（export 経路）
- 対処（実施済み）: `sanitizeTagPrefix` でタグ内 `[\r\n]` を除去し単一行を強制（両 tagChain）。markdownTagNewline.test.ts（4 tests）で攻撃経路を pin

### [Medium] PersistentRetryQueue の公開 load/save/getQueueSize が reject 契約を文書化していない
- 指摘者: Blue Team Leader（🎯 Data Integrity Expert も関連指摘）
- 対処（実施済み）: `load`/`save`/`getQueueSize`/`flush`/`flushBatch` の JSDoc に reject 契約と `[]` の三義性を明記
- [Medium] mutate が fn の throw とストレージ失敗を同一ログに畳む → 対処（実施済み）: ログ分離（`failed to apply queue mutation` / `failed to mutate queue`）+ queueMutateFailureKind.test.ts（3 tests）
- [Medium] saveMetadataStep.test.ts の mock が新 boolean 契約と矛盾 → 対処（実施済み）: mock を `true` 返却に修正 + 二重失敗テスト2件追加

### [Medium] enqueue/mutate の boolean 戻り値が上位で無視される（3箇所）
- 指摘者: Legacy Bridge Architect（🎯 Code Quality・Ops も指摘）
- 対処（実施済み）: `OfflineNetworkQueue.enqueue` を boolean 化（失敗時 ERROR）、`enqueuePendingRecord` を boolean 化して `saveSqliteStep` に二重失敗 ERROR 分岐を追加、`saveMetadataStep` は PBI 15 実装時に対応済み
- [Medium] flush が `[]` を三義（load 失敗/空キュー/正常完了）で返す → 対処（実施済み）: JSDoc で契約明記
- [Medium] callDashboard 内の同一待機式2箇所 / testConnection の 401・403・404 分岐重複 → 対処（実施済み）: `waitBetweenAttempts` ヘルパー抽出、`[401, 403, 404].includes` 一本化（挙動不変）
- [Medium] checkDomainWithRetry の手書き線形バックオフが SSOT 未採用 → 対処（実施済み）: 指数式のみ SSOT 化の判断をコメント化（線形は意図的）

## Low 指摘（修正せず記録）

- flush 時のアイテム単位 WARN の集約サマリ化（将来改善）
- `formatEntriesToGenericMarkdown` が alias なし削除（方針不統一）— PBI-13 の記録へ一行追記が望ましい
- 同名 `formatEntriesToMarkdown` が utils と dashboard の2モジュールに併存 — dashboard 側改名の影響調査が必要
- `describeHttpFailure` のドメイン分岐がマジック文字列依存 → 対処済み（`HttpFailureDomain` union 型化）
- 同一ステータスのドメイン別文言が byte-identical 固定化（意図的）／jitter なし（意図的見送り）／大フラッシュ時の WARN 件数
- System Architect: SettingsRepository 二重実体と未消費 container entry の残存、SSOT オプション行列の将来分岐再発余地、Layer 0 に製品ドメイン分岐を持つ文言 SSOT の配置

## コンフリクト調整結果

なし（相反する指摘は発生せず。System Architect は 100/100・Low 3件のみ）

## コードベース構造の知見（graphify）

- PersistentRetryQueue は degree 21 のハブ: `offlineNetworkQueue` / `pendingChromeStorageQueue` / `pendingSqliteQueue` の3ファサードが依存。PBI 15 の戻り型変更（void→boolean）は全ファサードに波及するため、Wave 3 の修正もファサード側に集中した
- キュー群は logger コミュニティに属する — `addLog` による構造化ログ連携は設計どおり（offlineNetworkQueue / pendingSqliteQueue が logger コミュニティ）
- markdownFormatter は SSOT 統合後に自前のコミュニティを形成（markdownJoinSafety / copyMarkdownButton / formatMarkdownStep が隣接）— サニタイズ列の変更はこの隣接群すべてに波及するため、改行インジェクション修正もこの SSOT 経由で全体に効いた
- no-masking の設計意図は archived PBI 2026-07-13-04（sqliteClient エラー伝播改善）まで遡れる系譜で、PBI 15 はその延長
- グラフには削除済みファイルのノードが残る可能性あり。矛盾時はコード実態を優先

## 対象外としてスキップした観点

- Accessibility Advocate（差分に UI コンポーネント・ARIA 変更なし。copyMarkdownButton の差分は import 改名2行のみを実読確認）
- Edge & Mobile Strategist（モバイル/PWA コードなし）
- API & Contract Negotiator（`QueuePort` / `QueueStorageAdapter` は SW 内部専用契約で外部公開 API に非該当。実装者と呼び出し側は同一ツリー内で共進化済み）

## 未完了の観点

なし
