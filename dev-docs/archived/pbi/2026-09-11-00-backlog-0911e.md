# Backlog 0911e — 2026-09-11 architecture review round 8

診断: HTML レポート `/var/folders/b_/fzr253l50g58s5p7d94nxjmc0000gn/T/architecture-review-20260911-2338-r8.html`（直接検証: **e2e 実行可能性が本環境で確認**（macOS + chromium install、extension.spec 12 passed / dashboard-ui.spec 52 passed）+ 1 explore subagent（fixture / helpers / selector / 交互テストパターンの詳細収集））。
入力: round 7 台帳の「e2e gap 2 spec」のトリガー発火 + 未踏領域の残り（e2e spec 作成に必要な詳細調査）。

## RICE スコアリング

```
RICE = (Reach × Impact × Confidence) / Effort
```

| NN | 候補 | R×I×C / E | RICE | 判断 |
|----|------|-----------|------|------|
| 01 | e2e spec: history panel UI（tag filter / pagination / star — round 6 監査 High gap） | 1×2×0.8 / 0.4 | **4.0** | PBI 化（test） |
| 02 | e2e spec: cleansing preview mask 遷移 → confirm → SAVE_RECORD（round 7 settle seam 改修の直下フロー） | 1×2×0.8 / 0.4 | **4.0** | PBI 化（test） |
| 03 | check-e2e の xvfb gate を platform-aware に（macOS で gate 誤 fail — playwright は動作することを実証済み） | 1×1×0.9 / 0.1 | **2.7** | PBI 化（fix） |
| — | 台帳トリガー駆動項目: archive codec 5 projections / testConnection template / Local AI session / DialogShell / trustSettings TagListController / archivePanel 5-concern 分離 / SqliteHistoryModel 21→8 / DashboardSqlite deps 3-seam / ErrorTaxonomy / offscreen-cleanse PoC（wire-or-delete） | — | — | **全件トリガー未発火**（git log + 現状で再確認）→ 維持 |
| — | popup settingsScreen 復活 | — | — | 製品判断待ち → 維持 |

## 依存グラフと実行順

```
01 (history panel spec) 独立
02 (cleansing preview spec) 独立
03 (check-e2e gate) — 01/02 の実行を release-checks でも通すため最後に実施してもよいが、spec 実行自体は playwright 直接で可能
```

実行順 = RICE 同点のため spec 実装順（01 → 02）→ 03。

## なぜなぜサマリー

- **なぜ e2e gap が 3 ラウンド残ったのか** → round 6/7 の監査で gap は指摘されていたが「check-e2e が xvfb 無しで skip になるため新規 spec を検証できない」と判断し、verify 可能な作業だけを回していた。→ 発見: xvfb-run の欠落は Linux 専用の制約で、macOS では playwright がそのまま動く。→ 解: 実行可能性を実証してから PBI 化する（本ラウンドのやり方）。
- **なぜ gate が xvfb-run を見るのか** → CI（Linux）の display server 前提で書かれたため。→ 解: platform-aware に（darwin/win32 は probe skip）。
