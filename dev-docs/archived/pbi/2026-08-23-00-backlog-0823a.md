# Backlog — 0823a Architecture Deepening 次ラウンド 8件 (RICE順)

## RICE スコア表

| 順位 | 候補 | Reach | Impact | Conf | Effort | RICE | 依存 | PBI |
|------|------|-------|--------|------|--------|------|------|-----|
| 1 | A: extractor 残り4責務 分割 | 9 | 3 | 80% | 1.0w | **720** | — | 01 |
| 2 | B: ServiceContainer 完全移行 | 8 | 2 | 75% | 1.0w | **480** | A後 | 02 |
| 3 | C: messageHandler 1層化 | 7 | 2 | 80% | 1.1w | **360** | — | 03 |
| 4 | D: settingsStore 退役 | 10 | 1.5 | 60% | 1.5w | **240** | B後 | 04 |
| 5 | E: systemHandlers 分割 | 5 | 1.5 | 80% | 1.3w | **180** | — | 05 |
| 6 | F: RecordingPipeline 分離 | 4 | 2 | 70% | 1.6w | **120** | B後 | 06 |
| 7 | G: returnInfo boolean trap | 6 | 1 | 80% | 2.0w | **96** | — | 07 |
| 8 | H: protocol SSOT 完全化 | 9 | 0.5 | 90% | 0.2w | **80** | — | 08 |

> **依存はスコアより優先**。実行順は上表の RICE 降順だが、B は A 完了後、D/F は B 完了後に着手。C/E/G/H は依存なしのため並行可能。

## 依存グラフ

```
A(720) ──→ B(480) ──→ D(240)
              └────→ F(120)
C(360)  E(180)  G(96)  H(80)  は独立（いつでも着手可）
```

## 5 Whys サマリー

- **A**: extractor.ts 593行に4責務が残留。PageState への書き込みが手動 parse で二重デフォルト。throttle が汎用なのに private。
- **B**: ServiceContainer 46行が孤立。createBackgroundServices は22 new + 27-field を手で配線。getSharedSqliteClient の二重生成はコメント注意のみ。
- **C**: messageHandler 6段if と MessageRouter trust/validator が二重チェック。CONTENT_SCRIPT_ONLY_TYPES(旧2要素) が残留し二重SSOT。
- **D**: SettingsRepository は polymorphic 済みだが34 call sites が依然 settingsStore 直import。ChromeStorageAdapter が dynamic import で循環。
- **E**: systemHandlers 11 handlers が bucket。recordingHandlers の visitRateLimiter が Map 共有だけで同居。
- **F**: RecordingPipeline 519行に steps宣言 + PerUrl Mutex + OfflineQueue が同居。retry loop が密結合。
- **G**: `extractMainContent(..., returnInfo: boolean)` が `string|ExtractResult` union を返し呼び出し元が毎回分岐。
- **H**: `wxt.config.ts:36 JSON.stringify(1)` がハードコードで protocol.ts と二重定義。

## Top recommendation

**A → C から**。A（content末端）とC（message入口）は依存なしで並行可能。両者で前回の残課題を同時解消。
