# Backlog 0912b — 2026-09-12 architecture deepening opportunities 8 件

round 10 診断（HTML レポート: `/var/folders/b_/fzr253l50g58s5p7d94nxjmc0000gn/T/architecture-review-20260912-1455-r10.html`）の deepening 候補 8 件を RICE 採点し PBI 化。実行順 = RICE 降順（09 → 16）。ハードな依存関係はなし（全候補ファイルクラスタが異なる）。

## RICE スコアリング

```
RICE = (Reach × Impact × Confidence) / Effort
```

Reach は開発者タッチ推定、Impact は 3=圧倒的 / 2=大 / 1=中、Confidence は行レベル検証済み 80% / 探索のみ 50%、Effort は人日。

| NN | 候補 | R×I×C / E | RICE | 判断 |
|----|------|-----------|------|------|
| 09 | TabBadgeResolver（判定二重実装の table 化） | 12×2×0.8 / 1.0 | **19.2** | PBI 化（refactor） |
| 10 | PreviewView stale-closure（実バグ） | 8×2×0.8 / 0.75 | **17.1** | PBI 化（fix） |
| 11 | Offline payload round-trip（04 の残渣回収） | 10×2×0.8 / 1.0 | **16.0** | PBI 化（refactor） |
| 12 | senderTrust 3 tier（router 漏出を回収） | 10×2×0.8 / 1.25 | **12.8** | PBI 化（refactor） |
| 13 | FallbackStorage alias 削除（backend 分岐解消） | 8×2×0.8 / 1.0 | **12.8** | PBI 化（refactor） |
| 14 | PreviewFlow payload builder + SpinnerScope | 10×2×0.8 / 1.5 | **10.7** | PBI 化（refactor） |
| 15 | DomainInputPolicy（検証二重化の単一化） | 8×2×0.8 / 1.25 | **10.2** | PBI 化（refactor） |
| 16 | Read-limit 単一 owner（planner が cap 選択） | 12×1×0.8 / 1.5 | **6.4** | PBI 化（refactor） |

同点（12/13）は「リスク軽減 → 緊急性」順。12 は security-adjacent のため先。

## 依存グラフと実行順

```
09 (tab badge) 独立
10 (preview nav) 独立
11 (offline round-trip) 独立（PBI 2026-09-12-04 の概念的フォローアップだがファイルは追加中心）
12 (sender trust) 独立
13 (fallback alias) 独立
14 (preview payload) 独立（10 と popup クラスタだがファイル非重複）
15 (domain policy) 独立
16 (read limit) 独立
```

実装は 09 → 16 の直列。

## なぜなぜサマリー

- **なぜ実バグ 2 件（10/13）が混ざるのか** → いずれも「正しさが呼び出し側の新鮮さ・経路に依存」する locality 欠落。→ 解: view が rewiring を所有 / planner を信頼して重複を削除し、pin テストで恒久化する。
- **なぜ 11 が 04 と別 PBI なのか** → 04 は request 側の統合で queue payload 側は残渣。別ファイル・別検証（round-trip）のため独立 PBI 化が正。
- **なぜ 12 が security-adjacent でも RICE 12.8 なのか** → 振る舞い変更なしのリファクタだが、sender 政策の散在は次の引き締めを 2 箇所編集にする。→ 解: table の 1 行化 + 順序 pin で将来の引き締めを 1 箇所に。
- **なぜ 16 が最下位なのか** → 実害ゼロの政策重複だが、コメント drift（plain:1000 表記）が既に発生しており放置で再発する。→ 解: 単一 owner + 対の drift 不能化で再発を構造的に防止。

## 台帳送り（本ラウンド不採択・`2026-09-05-00-backlog-future.md` に追加予定）

recordsRepo.serialize の interface 化（Strong だが Effort 大・export 列追加時に再評価）・IdbVfsBackend の withTransaction 越境 import（Worth・次回 backend 改修に同梱）・audit read path の orphan cap 配線（Worth・STATUS 改修時に同梱）・purge trust-boundary（Speculative・NaN bind 実測後に再評価）・StatusPanel 分割（Worth・次回 popup 改修時）・TagCluster overlay pipeline（Worth・視覚のみのため次回 tag 改修時）・single-flight 統合（Worth・4 つ目の consumer 出現時）・notification codec + reason ラベル（Worth・次回 notification 改修時）・VisitRateLimiter clock + TabCache twin（Speculative・次回該当改修時）
