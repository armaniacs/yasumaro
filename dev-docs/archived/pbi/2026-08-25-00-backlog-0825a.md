# Backlog — 0825a Architecture Deepening（staged Slice B/VisitGate適用後 6.7.78時点）

Phase 0 診断 0825a (2026-08-25 04:12) で6.7.78時点の残存摩擦を再評価。Slice B(63.0)/VisitGate(16.8)は完了、残りstagedとtype debtをRICE再計算。HTMLレポート: `/tmp/architecture-review-20260825041210.html`

## RICE スコア表

| 順位 | 候補 | Reach | Impact | Conf | Effort | RICE | 依存 | PBI |
|------|------|-------|--------|------|--------|------|------|-----|
| 1 | Slice C index撤廃+legacy削除 | 10 | 1.5 | 60% | 0.25w | **36.0** | Slice A後 | deferred Wave2 |
| 2 | Slice A Obsidian/AI/Privacy facade | 9 | 2 | 80% | 0.45w | **32.0** | — | 01 |
| 3 | F5 as unknown解消（CleansingConfig型安全） | 4 | 1 | 80% | 0.10w | **32.0** | — | 02 |
| 4 | F6 DI漏れ（pipeline/generator/router） | 7 | 1.5 | 75% | 0.35w | **22.5** | — | 03 |
| 5 | F4 Extractor god module残留 | 8 | 3 | 70% | 0.90w | **16.8** | — | deferred |

> **RICE = (Reach×Impact×Conf)/Effort**。Slice CはRICE 36.0で最上位だがSlice Aの完了を前提とするため実行順はA→C。残り3件（A/F5/F6）は依存なしで並列可能。旧0824dではSlice B 63.0が最優先だったが完了によりA/Cが昇格。

RICE降順の実行順は依存より優先: `Slice A(32.0) → Slice C(36.0)`、`F5(32.0)`はSlice Aと同点でリスク軽減を優先しF5をAと同ウェーブ。

## 依存グラフ

```
Slice A (32.0, 0.45w) ─→ Slice C (36.0, 0.25w) — A完了後に直列
F5 (32.0, 0.10w) — 独立（content vs storage）
F6 (22.5, 0.35w) — 独立（background vs storage/content）
F4 Extractor god module (16.8, 0.90w) — 独立だがEffort大で次スプリントへdeferred
```

ASCII依存図（今回実装分）:

```
PBI-01 Slice A (32.0) ─┐
PBI-02 F5 (32.0) ──┼─→ Wave 1 並列3（disjoint）
PBI-03 F6 (22.5) ──┘  Slice CはA完了後のWave 2へ
```

## 並列性調査

### ファイル触接集合

| PBI | 触るファイル | 規模 |
|-----|-------------|------|
| 01 Slice A | `src/utils/storage/SettingsRepository.ts` (facade 3本追加), `src/utils/storage/types.ts` (facade型), `src/background/serviceContainer.ts` (settingsRepository登録) | 3ファイル, ~120行 |
| 02 F5 | `src/content/pageState.ts:22` (CleansingConfig型), `src/content/extractor.ts:154-175` (as unknown 3箇所) | 2ファイル, ~30行 |
| 03 F6 | `src/background/createBackgroundServices.ts:144-218` (pipeline/generator/routerのcontainer登録), `src/background/serviceContainer.ts` (登録漏れ) | 2ファイル, ~80行 |
| deferred Slice C | `src/utils/storage/types.ts:469` (index signature), `src/utils/storage/settingsStore.legacy.ts` (削除), `eslint.config.js` | 3ファイル — Aと同一ファイル集合で衝突 |
| deferred F4 | `src/content/extractor.ts` 590行, `src/content/scrollTracker.ts`/`reportingService.ts` (新設) | 3ファイル — Effort大 |

**重なり判定:**
- PBI-01 vs PBI-02: disjoint ✅（storage vs content、importグラフ交差なし）
- PBI-01 vs PBI-03: disjoint ✅（storage vs background、types→serviceContainer via settingsRepository登録だが同時編集でもregister行のみで競合なし）
- PBI-02 vs PBI-03: disjoint ✅（content vs background）
- PBI-01 vs Slice C: 同一ファイル集合（types.ts/SettingsRepository.ts）で衝突 ❌ → 直列（CはA後のWave 2）
- PBI-03 vs  deferred F4: `createBackgroundServices.ts` vs `extractor.ts`でdisjointだがF4はEffort大で次スプリントへ

→ **Wave 1でPBI-01/02/03を並列実行可能**。ファイル重複0、マージ不要、同一作業ツリーで並列サブエージェントが安全。

### ウェーブ分割

| Wave | PBI | 並列/直列 | 理由 |
|------|-----|-----------|------|
| 1 | 01, 02, 03 | 並列3 | 依存なし + ファイルdisjoint、壁時計時間を0.45w→0.35wに短縮 |
| 2 | Slice C (36.0, 0.25w) | 直列 | Slice A完了後に直列で0.25w。Aと同一ファイル集合で並列不可 |
| deferred | F4 Extractor god module (16.8, 0.90w) | 次スプリント | Effort大 + VisitGate後の段階分割として次回でScrollTracker/ReportingServiceへ |

### クリティカルパス

Wave 1の最長EffortはSlice A 0.45w。壁時計時間の下限は0.45w（並列時）、直列なら0.90w（0.45+0.10+0.35）。Wave 2のSlice C 0.25wを含めたクリティカルパスはA→Cで0.70w。

## PBI再構成（分離・追加・マージ）

- **分離（split）**: 旧StorageKeys 2.0wをSlice B(0.20w完了)+Slice A(0.45w)+Slice C(0.25w)にseam境界で分離済み。Slice A/Cは独立したfacade/index撤廃とDoDを持ち、ファイル触接がA↔Cで同一だがWave分離で解消。F5(0.10w)はCleansingConfig型安全としてseparate seamで分離。
- **追加（add）**: なし。ServiceContainerは既に有効化PBIとして完了。
- **マージ（merge）**: Slice AとF5は同一RICE 32.0だがファイル集合がdisjointのため統合せず並列のまま。F4はEffort大で統合対象外。

## 5 Whys サマリー

- **Slice A**: なぜ47箇所が直参照か → Slice BでCleansingは集約されたがObsidian/AI/Privacyはfacade未実装で各所が`??`していたため → 解: 3 facadeで1 `getMany`に集約。
- **F5**: なぜ`as unknown` 3箇所か → `CleansingConfig`がexact型で`THRESHOLD_RULES`の`prop`代入が型エラーになるため → 解: `Record<ThresholdProp,number>`交差で型安全化。
- **F6**: なぜ後半7件がmanual newか → 前半10件はregister済みだがpipeline/generator/routerは直生成のままだったため → 解: containerに移し型を自動導出。

## Top recommendation

**PBI-01(Slice A, 32.0) + PBI-02(F5, 32.0) + PBI-03(F6, 22.5)をWave 1並列**。Slice AはServiceContainerで0.6w削減済みでCの前提、F5は0.10wの小粒高RICEで型債務を解消、F6は0.35wで後半DI漏れを塞ぐ。3件はstorage/content/backgroundでdisjoint、同ウェーブで壁時計時間を0.90w→0.45wに短縮。Wave 2でSlice C(A後)を0.25wで直列実施。
