# Backlog 0912f — 2026-09-12 architecture deepening opportunities 4 件

round 14 診断（HTML レポート: `/var/folders/b_/fzr253l50g58s5p7d94nxjmc0000gn/T/architecture-review-20260912-2010-r14.html`）の deepening 候補 4 件を RICE 採点し PBI 化。実行順 = RICE 降順（38 → 41）。ハードな依存関係はなし。

## RICE スコアリング

```
RICE = (Reach × Impact × Confidence) / Effort
```

Reach は開発者タッチ推定、Impact は 3=圧倒的 / 2=大 / 1=中、Confidence は行レベル検証済み 80%、Effort は人日。

| NN | 候補 | R×I×C / E | RICE | 判断 |
|----|------|-----------|------|------|
| 38 | IDB LIKE path qualified SQL 回帰修正（round 12 PBI 27 由来） | 15×2×0.8 / 0.75 | **32.0** | PBI 化（fix） |
| 39 | selectTagFilter（tag 導出 5 箇所の path-aware seam） | 8×1×0.8 / 0.75 | **8.5** | PBI 化（refactor） |
| 40 | tag/order cross-backend parity（6 入力発散 + corpus test） | 6×2×0.8 / 1.5 | **6.4** | PBI 化（fix） |
| 41 | StatusPanel split + 実バグ 3 件（trigger 発火済み） | 15×1×0.8 / 2.5 | **4.8** | PBI 化（refactor） |

## 依存グラフと実行順

```
38 (LIKE SQL fix) 独立
39 (selectTagFilter) 独立（38 の extra 射影分割と隣接するがファイル重複は軽微）
40 (tag parity) 40 は 39 の rowMatchesTagLike を利用するため 39 後が望ましい（依存は弱い）
41 (StatusPanel split) 独立
```

実装は 38 → 39 → 40 → 41 の直列（40 は 39 の seam を利用）。

## なぜなぜサマリー

- **なぜ 38 が round 12 由来の回帰なのか** → PBI 27 で extra を branch 前に 1 回構築する形に変更した際、LIKE path の SQL が別名なしであることを見落とした。params は位置同一なため string-assert テストも素通りした。→ 解: branch 毎射影 + real-engine 実行テストで恒久閉鎖。
- **なぜ 39 の rebuild が「単なる重複」でないのか** → FTS path の long tag には b.id 限定が、LIKE path の long tag には LIKE 条件が必須で、spec の再利用は cross case で破綻する。→ 解: 3-way selector で mapping を 1 箇所に。
- **なぜ 40 が silent wrong-answer 問題なのか** → fallback は degraded path backend だが、エラーでなく「間違った答え」を返すため顕在化が困難。→ 解: corpus test で prose（mirrors）を executable 契約に変換し、wildcard/case ポリシーを 1 回決定。
- **なぜ 41 を 2.5 人日もかけて実施するのか** → trigger 発火済み（round 12/13 で statusPanel を触った）+ 実バグ 3 件が同ファイルに残留。→ 解: 探索済みの 7 関数抽出 plan に従い、3 バグを同時解消。

## 台帳送り（本ラウンド不採択・`2026-09-05-00-backlog-future.md` に追加済み）

SingleFlight<K,V> 汎用化（Speculative 1-2d・VisitReporter の retry 中 drop 語彙が異なり要 caller buy-in）・QueueFacade receipt（Worth・次回 queue 改修時）・SqliteHistoryModel 3 split（Strong 3-5d・次回 history 改修時）・archivePanel extraction（Strong 3-4d・次回 archive 改修時）・cleanse race（statusPanel cleansing の last-writer-wins・sequence token が必要・StatusPanel split 後に再評価）

## 健全性確認（候補薄は正当な判定）

queryPlanner 6 plan 関数は god module 化していない（154 行）。messageHandler（94 行）は factory 1 個で分岐なし。exportEnvelope / supportsArchive seam は round 13 検証済み。previewView は round 13 PBI 37 で prune 済み。MANUAL/SAVE guard residual は round 13 で closed（分岐が本質的に異なる）。
