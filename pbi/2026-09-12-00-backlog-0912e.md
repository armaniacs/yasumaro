# Backlog 0912e — 2026-09-12 architecture deepening opportunities 5 件

round 13 診断（HTML レポート: `/var/folders/b_/fzr253l50g58s5p7d94nxjmc0000gn/T/architecture-review-20260912-1700-r13.html`）の deepening 候補 5 件を RICE 採点し PBI 化。実行順 = RICE 降順（33 → 37）。ハードな依存関係はなし。

## RICE スコアリング

```
RICE = (Reach × Impact × Confidence) / Effort
```

Reach は開発者タッチ推定、Impact は 3=圧倒的 / 2=大 / 1=中、Confidence は行レベル検証済み 80%、Effort は人日。

| NN | 候補 | R×I×C / E | RICE | 判断 |
|----|------|-----------|------|------|
| 33 | CacheInitializedFlag restore-once（round 12 PBI 25 half-fix + Proxy echo） | 25×1×0.8 / 0.5 | **40.0** | PBI 化（fix） |
| 34 | reason-label canonical 解決（raw slug 表示実バグ） | 10×2×0.8 / 0.5 | **32.0** | PBI 化（fix） |
| 35 | filter 語彙統合（search+ids nested bind 実バグ解消） | 15×2×0.8 / 1.0 | **24.0** | PBI 化（refactor） |
| 36 | planPurge(0,0) old-records 契約（round 12 PBI 26 の追半分） | 6×2×0.8 / 0.5 | **19.2** | PBI 化（fix） |
| 37 | PreviewView dead interface prune | 4×0.5×0.8 / 0.25 | **6.4** | PBI 化（refactor） |

## 依存グラフと実行順

```
33 (flag restore-once) 独立
34 (reason label) 独立
35 (filter vector) 独立
36 (purge old-records) 独立（round 12 PBI 26 の追半分）
37 (previewView prune) 独立
```

実装は 33 → 37 の直列。

## なぜなぜサマリー

- **なぜ round 12 の「half-fix」が 2 件も発生したのか** → PBI 25/26/31 がいずれも「seam の新設」までを検証し、「全 call site の移行完了」を DoD に入れていなかった。→ 解: 本ラウンドで残半分を閉じ、以降の seam 新設 PBI は「移行完了」を DoD に含める。
- **なぜ 33 が 40.0 なのか** → reach が全メッセージ（25）で Proxy echo という構造的欠陥が乗っている。→ 解: RestoreOnce 紀律を flag にも適用し Proxy を廃止。
- **なぜ 34 が利用者可視なのか** → locale が canonical を出荷しているのに legacy-only 解決のため raw slug が表示される。→ 解: resolveReasonLabel adapter に統一。
- **なぜ 35 が検証をすり抜けたのか** → Ssot テストの assert 側が flatMap で flatten しており production 出力を assert していなかった（テストが production を mirror していなかった）。→ 解: flatten assert + search+ids round-trip で恒久化。
- **なぜ StatusPanel split（Strong 2-3d）を外したのか** → Effort 大・trigger は発火済みだが round 13 の他候補が RICE で圧倒。次回 popup 改修時の台帳項目として維持。

## 台帳送り（本ラウンド不採択・`2026-09-05-00-backlog-future.md` に追加済み）

StatusPanel split（Strong 2-3d・次回 popup 改修時）・search-path tag condition 再構築（Worth 0.5-1d）・SingleFlight<K,V> 汎用化（Speculative 1-2d）・FallbackStorage.query spec 未使用部分の文書化（Speculative 0.5-1d）・MANUAL/SAVE guard residual（台帳から削除 — round 13 再評価で「分岐は本質的に異なる、統合は branch flag を足すだけ」と判定）

## 健全性確認（候補薄は正当な判定）

queryPlanner 6 plan 関数は god module 化していない（154 行・wire-to-backend trust boundary の意図的 SSOT・deletion test で全 call site が再派生を強いられる = depth あり）。messageHandler / exportEnvelope / supportsArchive seam も健全。次回 queryPlanner 再評価トリガー: 250 行超過か新 planning domain（archive/export）追加時。
