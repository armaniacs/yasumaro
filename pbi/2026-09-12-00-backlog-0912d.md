# Backlog 0912d — 2026-09-12 architecture deepening opportunities 8 件

round 12 診断（HTML レポート: `/var/folders/b_/fzr253l50g58s5p7d94nxjmc0000gn/T/architecture-review-20260912-1640-r12.html`）の deepening 候補 8 件を RICE 採点し PBI 化。実行順 = RICE 降順（25 → 32）。ハードな依存関係はなし。

## RICE スコアリング

```
RICE = (Reach × Impact × Confidence) / Effort
```

Reach は開発者タッチ推定、Impact は 3=圧倒的 / 2=大 / 1=中、Confidence は行レベル検証済み 80%、Effort は人日。

| NN | 候補 | R×I×C / E | RICE | 判断 |
|----|------|-----------|------|------|
| 25 | メッセージ毎 restore ファンアウト削減（round 11 prune の hot-path 回帰） | 25×1×0.8 / 0.5 | **40.0** | PBI 化（fix） |
| 26 | planPurge 0-vs-absent 契約確定（2 op が逆動作） | 6×2×0.8 / 0.5 | **19.2** | PBI 化（fix） |
| 27 | Filter condition SSOT（SQL search の excludeDeleted 無視解消） | 15×2×0.8 / 1.5 | **16.0** | PBI 化（refactor） |
| 28 | Fallback export 10k 切り詰め | 4×2×0.8 / 0.5 | **12.8** | PBI 化（fix） |
| 29 | 小型バグバンドル 4 件（statusToggleBtn 二重配線・removeAll N flush・gate! pre-init・tagClusterLoading labels） | 15×1×0.8 / 1.0 | **12.0** | PBI 化（fix） |
| 30 | SingleFlight<K>（5 スペリング統合） | 15×1×0.8 / 1.5 | **8.0** | PBI 化（refactor） |
| 31 | NotificationCodec + ReasonLabel（multi-hyphen latent bug 解消） | 8×1×0.8 / 1.0 | **6.4** | PBI 化（refactor） |
| 32 | supportsArchive narrowing 統一 | 4×0.5×0.8 / 0.25 | **6.4** | PBI 化（refactor） |

同点（31/32）は「リスク軽減 → 緊急性」順。31 は multi-hyphen latent bug 解消（リスク軽減）で先。

## 依存グラフと実行順

```
25 (restore cost) 独立
26 (purge contract) 独立
27 (filter SSOT) 独立
28 (fallback export) 独立
29 (small bundle) 独立
30 (SingleFlight) 独立（PBI 20 の contextMenu keyed 化を前提にしない）
31 (codec + labels) 独立
32 (supportsArchive) 独立
```

実装は 25 → 32 の直列。

## なぜなぜサマリー

- **なぜ 25 が首位なのか** → round 11 PBI 24 の prune port が restore を fan-out 型に変え、全メッセージの先頭に乗ってしまった。regression の早急解消であり reach が全メッセージ。→ 解: restore の発火点を起動 + tabRemoved に限定する。
- **なぜ 26 が 19.2 もあるのか** → 破壊的操作 2 件が同じ planner 出力で正反対に動く（purgeOldRecords(0)=全件削除 vs purgeContent(0)=無視）は契約の欠落。→ 解: 0 を「スキップ」に確定し parametric で pin。
- **なぜ 27 が 1.5 人日かかるのか** → buildExtraWhereSql の `b.` 限定子を regex replace から param 化への転換が必要なため。→ 解: 構造化条件 builder 1 箇所に集約し parity テストで pin。
- **なぜ SqliteHistoryModel split（Strong・3-5d）と archivePanel extraction（Strong・3-4d）を外したのか** → Effort 大に対し Reach が panel 単一。次回該当 panel 改修時の台帳項目として維持。
- **なぜ 30 が 5 site もあるのに I1 なのか** → 既存 5 実装は動作しておりバグでない。次の 6 番目のスペリングを防ぐ政策統合として評価。

## 台帳送り（本ラウンド不採択・`2026-09-05-00-backlog-future.md` に追加済み）

SqliteHistoryModel 3 module split（Strong・3-5d・次回 history 改修時）・archivePanel extraction（Strong・3-4d・次回 archive 改修時）・StatusPanel split（Worth・501 行・次回 popup 改修時）・TagCluster pipeline（Worth・次回 tag 改修時）・QueueFacade receipt（Worth・2 テレメトリ race は under-report 化済み・次回 queue 改修時）・MANUAL/SAVE guard residual（Worth 0.5d・次回 recording 改修時）・VisitRateLimiter clock（Speculative・変化なし）・trustSettings TagListController + previewView dead members（Worth 小バッチ・次回該当改修時）
