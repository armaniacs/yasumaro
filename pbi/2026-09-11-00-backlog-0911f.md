# Backlog 0911f — 2026-09-11 architecture deepening opportunities 5 件

アーキテクチャレポートの deepening 候補 5 件を RICE 採点し PBI 化。実行順 = RICE 降順（04 → 05 → 06 → 07 → 08）。ハードな依存関係はなし（05→07 のみ順序推奨）。

## RICE スコアリング

```
RICE = (Reach × Impact × Confidence) / Effort
```

Reach は四半期あたりの開発者タッチ推定（ホットパスほど大）、Impact は 3=圧倒的 / 2=大きい / 1=中、Effort は人日。全候補を同一基準で相対比較。

| NN | 候補 | R×I×C / E | RICE | 判断 |
|----|------|-----------|------|------|
| 04 | RecordingOrchestrator を 3 エントリに（Strong） | 25×2×0.8 / 1 | **40.0** | PBI 化（refactor） |
| 05 | SQLite read path を QueryPlanner に（Strong） | 30×2×0.8 / 2 | **24.0** | PBI 化（refactor） |
| 06 | StorageBackend から archive seam 分離（Strong） | 20×2×0.8 / 2.5 | **12.8** | PBI 化（refactor） |
| 07 | dashboard→offscreen dispatch 連鎖畳み込み（Worth exploring） | 30×2×0.5 / 3 | **10.0** | PBI 化（refactor） |
| 08 | popup permission ladder 抽出（Speculative） | 10×1×0.5 / 2 | **2.5** | PBI 化（refactor） |

## 依存グラフと実行順

```
04 (orchestrator narrow) 独立
05 (query planner) ──推奨先行──> 07 (dispatch collapse) ※07 は 05 の内側 seam にルーティングさせる
06 (archive seam) 独立
08 (permission ladder) 独立（いつでも着手可だが最後）
```

実行順 = 04 → 05 → 06 → 07 → 08。05 と 07 は同一 read フローの内外層のため直列、他はファイル非重複を確認の上で並列化可。

## なぜなぜサマリー

- **なぜ Strong 3 件が上位なのか** → いずれも Confidence 80% かつ Effort が見積り可能で、触る seam と呼び出し元が特定済みだから。→ 解: 先行実施し、探索要素のある 07・08 の前に確実な価値を確定させる。
- **なぜ 07 は Worth exploring なのに PBI 化するのか** → validation＋projection の分離難度が未確定だが、Reach（dashboard 読み取り全体）が大きく、05 完了後なら内側 seam が安定して分離難度が下がるから。→ 解: 05 の後に着手し、着手前スパイクで 3 人日超過見込みなら分割する（PBI 内に明記）。
- **なぜ 08 は最下位なのか** → Speculative かつ seam を正当化する adapter が 2 件のみで、他候補より波及が小さいから。→ 解: 最後に回し、着手前に seam 正当化を再確認し、できない場合は見送る（PBI 内に明記）。
- **なぜ archive 分離（06）が 3 位なのか** → 効果（30→16 に接口縮小）は大きいが、14 操作の移設＋呼び出し元更新で Effort が 04・05 より大きいから。→ 解: 04・05 の後に実施し、round 3/4 の archive 前提（wire table・共有 stub）を壊さないよう PBI 内に制約を明記した。
- **なぜ 05 を 07 より先にするのか** → 05 は内側（normalize-to-decode）、07 は外側（validation・routing・projection）で、07 が 05 の seam にルーティングする関係だから。→ 解: 05→07 の順序を依存として記録した。
