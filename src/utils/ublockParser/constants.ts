/**
 * ublockParser/constants.ts
 * uBlock Origin形式フィルターパーサーの定数定義
 *
 * 【機能概要】: パーサーで使用するすべての定数を集約
 * 🟢 信頼性レベル: plan/UII/02-phase2-parser.md および plan/UII/10-data-structures.md に基づく実装
 */

// ============================================================================
// キャッシュ定数
// ============================================================================

/** 【キャッシュ定数】: パーサーキャッシュの設定 🟢 */
export const CACHE_CONFIG = {
  /** キャッシュの最大サイズ */
  MAX_SIZE: 100,
  /** LRUキャッシュの最大エントリ数 */
  LRU_MAX_ENTRIES: 50
} as const;

// LRUキャッシュのクリーンアップ間隔（ミリ秒）
export const CLEANUP_INTERVAL = 300000; // 5分

// ============================================================================
// 正規表現定数
// ============================================================================

/** 【正規表現定数】: uBlock形式の基本パターンマッチング 🟢 */
export const PATTERNS = {
  /** `||` プレフィックス検出 */
  RULE_PREFIX: /^\|\|/,
  /** `^` サフィックス検出（行末） */
  RULE_SUFFIX: /\^$/,
  /** 空行検出（空白のみ含む） */
  EMPTY_LINE: /^\s*$/,
  /** `!` コメントプレフィックス検出 */
  COMMENT_PREFIX: /^!/,
  /** `#` コメントプレフィックス検出（hosts形式） */
  HOSTS_COMMENT_PREFIX: /^#/,
  /** hosts形式検出: IPアドレス（IPv4/IPv6、ゾーンID付きIPv6を含む）で始まる行 */
  HOSTS_FORMAT: /^((?:[0-9]{1,3}\.){3}[0-9]{1,3}|[a-fA-F0-9:]+(?:%[a-zA-Z0-9]+)?|255\.255\.255\.255)\s+(.+)$/,
  // ドメイン形式検証はネストした量指定子で ReDoS の原因になっていたため削除（VULN-025）。
  // `validation.ts` の validateDomain がラベル単位の線形検証で置き換えている。
} as const;

// ============================================================================
// ルールタイプ定数
// ============================================================================

/** 【ルールタイプ定数】: ルールの種類を表す文字列定数 🟢 */
export const RULE_TYPES = {
  /** ドメインをブロックするルール */
  BLOCK: 'block',
  /** ブロックの例外として許可するルール */
  EXCEPTION: 'exception',
  /** 無視するルール（hostsファイルのlocalhost定義など） */
  IGNORE: 'ignore',
} as const;

export type RuleType = typeof RULE_TYPES[keyof typeof RULE_TYPES];

// ============================================================================
// オプション種別定数
// ============================================================================

/** 【オプション種別定数】: uBlock形式のオプション識別子 🟢 */
export const OPTION_TYPES = {
  /** ドメイン指定プレフィックス `domain=` */
  DOMAIN_PREFIX: 'domain=',
  /** サードパーティフラグ `3p` */
  THIRD_PARTY: '3p',
  /** ファーストパーティフラグ `1p` */
  FIRST_PARTY: '1p',
  /** 重要フラグ `important` */
  IMPORTANT: 'important',
  /** 重要フラグ解除 `~important` */
  NOT_IMPORTANT: '~important',
  /** 大文字小文字を区別する `match-case` */
  MATCH_CASE: 'match-case',
  /** 大文字小文字を区別しない `~match-case` */
  NOT_MATCH_CASE: '~match-case',
  /** 除外ドメインプレフィックス */
  EXCLUDE_DOMAIN_PREFIX: '~',
  /** ドメイン区切り記号 */
  DOMAIN_SEPARATOR: '|',
  /** オプション区切り記号 */
  OPTION_SEPARATOR: ',',
} as const;

// ============================================================================
// プレフィックス定数
// ============================================================================

/** 【プレフィックス定数】: uBlock形式のプレフィックス 🟢 */
export const PREFIXES = {
  /** ルールプレフィックス */
  RULE: '||',
  /** 例外ルールプレフィックス */
  EXCEPTION: '@@||',
  /** サフィックス */
  SUFFIX: '^',
  /** オプション開始 */
  OPTION: '$',
} as const;

// ============================================================================
// メタデータ定数
// ============================================================================

/** 【メタデータ定数】: デフォルトのメタデータ値 🟢 */
export const DEFAULT_METADATA = {
  /** デフォルトデータソース */
  SOURCE: 'paste',
} as const;

// ============================================================================
// ルールID定数
// ============================================================================

/** 【ルールID定数】: null/undefined入力時に返す固定ID 🟡 */
export const NULL_RULE_ID = '00000000-0000-0000-0000-000000000000';